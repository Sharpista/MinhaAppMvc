@page
@model DevIO.App.ViewModel.FornecedorViewModel

@using (Html.BeginForm("Create", "Fornecedores", FormMethod.Post))
{
        <div class="row">
          <div class="col">
            <input type="text" class="form-control" placeholder="First name" aria-label="First name">
          </div>
          <div class="col">
            <input type="text" class="form-control" placeholder="Last name" aria-label="Last name">
          </div>
        </div>
        <button class="btn-primary" type="submit">CLICA AI PORRA</button>
}

